Place your frontend HTML files here:
  - index.html   (landing / auth page)
  - doctor.html  (coordinator dashboard)
  - patient.html (patient portal)

These are served by FastAPI at:
  GET /          → index.html
  GET /doctor    → doctor.html
  GET /patient   → patient.html
